export type ProductType = {
    id: number,
    img: string,
    title: string,
    desc: string,
    bg: string,
}

export type Category = {
    id: number,
    img: string,
    title: string,
    cat:string,
}

export type popularProduct = {
    id: number,
    img: string,
}