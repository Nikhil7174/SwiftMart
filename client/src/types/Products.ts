export type Product = {
    id: number,
    img: string,
    title: string,
    desc: string,
    bg: string,
}

export type Category = {
    id: number,
    img: string,
    title: string,
}

export type popularProduct = {
    id: number,
    img: string,
}